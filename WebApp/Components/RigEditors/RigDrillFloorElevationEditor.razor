@namespace WebApp.Components.RigEditors

<RigDoublePropertyEditor Title="Drill Floor Elevation"
                         Quantity="LengthStandard"
                         Value="Rig.DrillFloorElevation"
                         ValueChanged="@(value => Rig.DrillFloorElevation = value)" />

@code {
    [Parameter, EditorRequired] public Rig Rig { get; set; } = default!;
}
