apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillingrigservice.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillingrigservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillingrigservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
