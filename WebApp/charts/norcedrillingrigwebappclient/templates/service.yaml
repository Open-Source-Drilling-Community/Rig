apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillingrigwebappclient.fullname" . }}
  labels:
    {{- include "norcedrillingrigwebappclient.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillingrigwebappclient.selectorLabels" . | nindent 4 }}
